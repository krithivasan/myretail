akka {
  actor {
    deployment {
      /productInfoActor {
        router = round-robin-pool
        nr-of-instances = 10
      }
    }
  }
  http{
    host-connection-pool{
      max-connections = 1024
    }
  }
}